# wordpress-clean-malicious-htaccess
🧹 PHP script to detect and bulk-remove malicious .htaccess files injected across WordPress after a server compromise. Zero false positives - exact MD5 fingerprint matching. Dry-run mode by default. Works via CLI (SSH) or browser (shared hosting).
